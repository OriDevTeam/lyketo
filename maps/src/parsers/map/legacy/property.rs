// Standard Uses

// Crate Uses
use crate::formats::map::property::Property;

// External Uses


pub fn from_file(_path: &str) -> Property {
    let _property = Property::new();
    _property
}

