// Standard Uses

// Crate Uses

// External Uses
use super::vertex::Vertex;


pub struct Segment {
    pub vertices: Vec<Vertex>
}


impl Segment {
    /*
    fn generate(self, size: Rectangle) -> Segment {
        let segment = Segment {
            positions: todo!(),
            normal_position: todo!(),
            texture_coordinates: todo!(),
            color: todo!(),
        };

        return segment
    }
     */
    
     /*
    fn position(self) -> Axis {
        return self.positions;
    }
     */

}
